// 4️⃣9️⃣ Check if character is vowel or consonant
let char = 'a';
if(char === 'a' || char === 'e' || char === 'i' || char === 'o' || char === 'u' || char === 'A' ||char === 'E' || char === 'I' || char === 'O' ||char === 'U' ){
    console.log(`${char} is a vowel`);
} else {
    console.log(`${char} is a consonant`);
}
// output: a is a vowel