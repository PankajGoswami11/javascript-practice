// 04: Difference between push() and unshift()

// push() → adds element at the end
// unshift() → adds element at the beginning

let arr = [1, 2, 3];

arr.push(4);     // [1, 2, 3, 4]
arr.unshift(0);  // [0, 1, 2, 3, 4]

console.log(arr);
